# URL-Shortener


"""

routes.py → Receives HTTP requests and returns responses.
services.py → Contains the business logic (generate codes, save URLs, etc.).
models.py → Defines the database tables.
utils.py → Helper functions, like generating a random short code.

"""